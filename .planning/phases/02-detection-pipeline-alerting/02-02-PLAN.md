---
phase: 02-detection-pipeline-alerting
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - arp_detector/logger.py
  - tests/test_logger.py
autonomous: true
requirements:
  - DET-05
  - ALT-03
  - ALT-04
must_haves:
  truths:
    - "JSONLLogger.log_event() appends exactly one JSON line per call"
    - "Each written line is parseable with json.loads() with no errors"
    - "The log file is non-empty (flushed to disk) immediately after log_event() returns"
    - "JSONLLogger.close() flushes and closes the file handle without error"
    - "build_event() returns a dict with all six DET-05 keys present"
    - "build_event() timestamp field is an ISO 8601 string (contains T separator)"
    - "build_event() attacker_mac == conflict['new_mac'] and victim_ip == conflict['ip']"
  artifacts:
    - path: "arp_detector/logger.py"
      provides: "JSONLLogger class + build_event() function"
      exports: ["JSONLLogger", "build_event"]
    - path: "tests/test_logger.py"
      provides: "Unit tests for DET-05 and ALT-03/ALT-04 — uses tmp_path fixture, no root required"
  key_links:
    - from: "arp_detector/logger.py"
      to: "arp_detector.log (default path)"
      via: "open(path, 'a', encoding='utf-8') in JSONLLogger.__init__"
      pattern: "open.*\"a\""
    - from: "build_event"
      to: "conflict dict from ARPTable.update()"
      via: "direct key mapping — conflict['new_mac'] -> attacker_mac, etc."
      pattern: "conflict\\['new_mac'\\]"
---

<objective>
Implement arp_detector/logger.py with two components:
  1. JSONLLogger class — opens log file in append mode, writes one JSON line per event,
     flushes immediately after each write, closes cleanly on shutdown.
  2. build_event() function — transforms the ARPTable conflict dict into the locked
     DET-05 event dict (6 required keys).

Purpose: Persistent JSONL log that is readable with `cat` between events, and the canonical
event dict shape that alerts.py and main.py will both consume.
Output: logger.py module + test suite covering ALT-03, ALT-04, DET-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@arp_detector/arp_table.py

<interfaces>
<!-- Locked DET-05 event dict shape — from CONTEXT.md and RESEARCH.md -->

Conflict dict from ARPTable.update() (input to build_event):
```python
{'ip': str, 'known_mac': str, 'new_mac': str, 'timestamp': float}
```

DET-05 event dict (output of build_event — LOCKED field set):
```python
{
    'timestamp':    str,   # datetime.fromtimestamp(conflict['timestamp']).isoformat()
    'attacker_mac': str,   # conflict['new_mac']
    'victim_ip':    str,   # conflict['ip']
    'original_mac': str,   # conflict['known_mac']
    'spoofed_mac':  str,   # conflict['new_mac']  (same as attacker_mac per spec)
    'attack_type':  str,   # always "ARP_SPOOFING"
}
```

JSONLLogger class shape:
```python
class JSONLLogger:
    def __init__(self, path: str = "arp_detector.log") -> None: ...
    def log_event(self, event: dict) -> None: ...   # write + flush
    def flush(self) -> None: ...                    # explicit flush
    def close(self) -> None: ...                    # flush + close file handle
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for logger.py (RED)</name>
  <files>tests/test_logger.py</files>
  <read_first>
    - arp_detector/arp_table.py — conflict dict shape returned by update()
    - tests/test_detector.py — existing test style to follow
  </read_first>
  <behavior>
    test_log_event_appends_one_line: create JSONLLogger(tmp_path/"test.log"), call log_event
      once with a sample event dict, read file lines, assert len(lines) == 1.

    test_log_event_valid_json: same setup, assert json.loads(lines[0]) equals the event dict
      passed to log_event.

    test_log_event_flushed_immediately: after log_event, open the same file path again
      independently (new file handle), assert content is not empty — proves flush happened
      without calling close().

    test_two_events_two_lines: call log_event twice with different events, assert file has
      exactly 2 lines, each parseable with json.loads().

    test_close_no_error: call close() on a logger with 1 written event — must not raise any
      exception.

    test_close_file_handle_closed: after close(), assert logger._fh.closed is True.

    test_build_event_keys: call build_event({'ip':'1.2.3.4','known_mac':'aa','new_mac':'bb',
      'timestamp':1000.0}) — assert returned dict has exactly these keys:
      {'timestamp','attacker_mac','victim_ip','original_mac','spoofed_mac','attack_type'}.

    test_build_event_timestamp_iso: assert result['timestamp'] contains 'T'
      (ISO 8601 format: "1970-01-01T00:16:40" shape).

    test_build_event_attacker_mac: assert result['attacker_mac'] == 'bb' (the new_mac).

    test_build_event_victim_ip: assert result['victim_ip'] == '1.2.3.4' (the ip).

    test_build_event_original_mac: assert result['original_mac'] == 'aa' (the known_mac).

    test_build_event_spoofed_mac: assert result['spoofed_mac'] == 'bb' (same as attacker_mac).

    test_build_event_attack_type: assert result['attack_type'] == 'ARP_SPOOFING'.

    All tests use pytest's tmp_path fixture for log files — no hardcoded paths.
    No mocking of subprocess; no root; no network.
  </behavior>
  <action>
    Create tests/test_logger.py with the above test cases. Tests import
    `from arp_detector.logger import JSONLLogger, build_event` — these do not exist yet
    in the stub, so all tests will fail (ImportError or AttributeError = RED).
    Commit: `test(02-02): add failing tests for logger.py (RED)`
  </action>
  <verify>
    <automated>python -m pytest tests/test_logger.py -v 2>&1 | head -30</automated>
  </verify>
  <done>All tests in test_logger.py fail — RED phase confirmed.</done>
  <acceptance_criteria>
    - grep -n "from arp_detector.logger import JSONLLogger, build_event" tests/test_logger.py
    - grep -n "tmp_path" tests/test_logger.py
    - grep -n "json.loads" tests/test_logger.py
    - grep -n "test_build_event_keys" tests/test_logger.py
    - python -m pytest tests/test_logger.py -q 2>&1 | grep -E "ERROR|FAILED|error"
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Implement logger.py to make tests pass (GREEN)</name>
  <files>arp_detector/logger.py</files>
  <read_first>
    - tests/test_logger.py — the exact tests that must pass
    - arp_detector/arp_table.py — conflict dict shape (input to build_event)
  </read_first>
  <behavior>
    build_event(conflict: dict) -> dict:
      - Return the DET-05 event dict with exactly 6 keys as specified
      - timestamp: datetime.fromtimestamp(conflict['timestamp']).isoformat()
      - attacker_mac: conflict['new_mac']
      - victim_ip: conflict['ip']
      - original_mac: conflict['known_mac']
      - spoofed_mac: conflict['new_mac']
      - attack_type: "ARP_SPOOFING"

    JSONLLogger class:
      - __init__(self, path: str = "arp_detector.log") -> None
        Open file: self._fh = open(path, "a", encoding="utf-8")
      - log_event(self, event: dict) -> None
        self._fh.write(json.dumps(event) + "\n")
        self._fh.flush()   # MANDATORY — ensures file readable between events
      - flush(self) -> None
        self._fh.flush()
      - close(self) -> None
        self._fh.flush()
        self._fh.close()

    The module-level stub functions log_event() and flush() from the original stub
    must be REPLACED entirely — the class-based approach is the implementation.
    Do not keep the old stub functions; they will confuse importers.

    Imports needed: json, sys (for future use), datetime from datetime,
    pathlib.Path (optional), typing (optional).
  </behavior>
  <action>
    Completely replace the contents of arp_detector/logger.py with the full implementation.
    The file must export both JSONLLogger and build_event at module level.
    Module docstring must reference DET-05, ALT-03, ALT-04.
    After implementation, run tests. Commit only when all pass:
    `feat(02-02): implement logger.py — JSONLLogger + build_event (GREEN)`
  </action>
  <verify>
    <automated>python -m pytest tests/test_logger.py -v</automated>
  </verify>
  <done>All test_logger.py tests pass. Full suite still passes (no regressions).</done>
  <acceptance_criteria>
    - grep -n "class JSONLLogger" arp_detector/logger.py
    - grep -n "def build_event" arp_detector/logger.py
    - grep -n "_fh.flush()" arp_detector/logger.py
    - grep -n "ARP_SPOOFING" arp_detector/logger.py
    - grep -n "isoformat()" arp_detector/logger.py
    - python -m pytest tests/test_logger.py -v 2>&1 | grep -E "passed|PASSED"
    - python -m pytest tests/ -q 2>&1 | tail -3
  </acceptance_criteria>
</task>

</tasks>

<verification>
Full suite passes with no regressions:

```
python -m pytest tests/ -v
```

The locked DET-05 contract is verified by test_build_event_keys asserting all 6 keys
are present and correctly mapped from the conflict dict.
</verification>

<success_criteria>
- arp_detector/logger.py exports JSONLLogger class and build_event() function
- JSONLLogger.log_event() writes + flushes on every call
- JSONLLogger.close() leaves file handle in closed state
- build_event() maps conflict dict to all 6 DET-05 keys correctly
- All test_logger.py tests pass
- Full test suite green (no regressions)
- DET-05, ALT-03, ALT-04 (partial — flush on close) requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-detection-pipeline-alerting/02-02-SUMMARY.md`
</output>
