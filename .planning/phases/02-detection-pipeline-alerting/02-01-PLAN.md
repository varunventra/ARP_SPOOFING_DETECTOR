---
phase: 02-detection-pipeline-alerting
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - arp_detector/baseline.py
  - tests/test_baseline.py
autonomous: true
requirements:
  - DET-02
must_haves:
  truths:
    - "load_baseline() runs arp-scan --localnet via subprocess (list-form args, no shell=True)"
    - "load_baseline() seeds ARPTable entries for every valid IP/MAC data line in arp-scan output"
    - "load_baseline() skips arp-scan header and footer lines without crashing"
    - "load_baseline() returns count of hosts loaded as an integer"
    - "load_baseline() prints a warning to stderr and returns 0 when arp-scan binary is not found"
    - "load_baseline() prints a warning to stderr and returns 0 on subprocess timeout"
  artifacts:
    - path: "arp_detector/baseline.py"
      provides: "arp-scan subprocess wrapper, load_baseline(table, timeout) -> int"
      exports: ["load_baseline"]
    - path: "tests/test_baseline.py"
      provides: "Unit tests covering DET-02 — all tests mock subprocess.run, no root required"
  key_links:
    - from: "arp_detector/baseline.py"
      to: "arp_detector/arp_table.ARPTable.update"
      via: "direct call per parsed IP/MAC pair"
      pattern: "table\\.update\\("
    - from: "tests/test_baseline.py"
      to: "subprocess.run"
      via: "unittest.mock.patch"
      pattern: "patch.*subprocess\\.run"
---

<objective>
Implement arp_detector/baseline.py — the arp-scan subprocess wrapper that seeds the ARPTable
before the sniffer starts. Without this, the first ARP reply from any known host triggers a
false positive.

Purpose: Pre-populate ARPTable so known hosts are not flagged as attackers on first sight.
Output: baseline.py module with load_baseline() function + full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@arp_detector/arp_table.py
@arp_detector/detector.py

<interfaces>
<!-- Existing contracts the executor must use directly — no codebase exploration needed. -->

From arp_detector/arp_table.py:
```python
class ARPTable:
    def update(self, ip: str, mac: str) -> dict | None:
        """Record or update IP->MAC mapping. Returns None or conflict dict."""

    def get_all(self) -> pd.DataFrame:
        """Return snapshot copy. Index=ip, columns=[mac, first_seen, last_seen]."""
```

Packet dict shape (produced by capture.py, NOT needed here but for reference):
```python
{'src_ip': str, 'src_mac': str, 'dst_ip': str, 'op': int, 'timestamp': float}
```

Conflict dict shape (returned by ARPTable.update when conflict):
```python
{'ip': str, 'known_mac': str, 'new_mac': str, 'timestamp': float}
```

Subprocess pattern (from CLAUDE.md — mandatory):
```python
result = subprocess.run(
    ["arp-scan", "--localnet"],
    capture_output=True, text=True, timeout=30,
)
# NEVER shell=True
```

arp-scan output to parse (MEDIUM confidence — confirmed tab-separated):
```
Interface: eth0, type: EN10MB, MAC: aa:bb:cc:00:00:01, IPv4: 192.168.1.1
Starting arp-scan 1.9.7 with 256 hosts
192.168.1.1\taa:bb:cc:dd:ee:01\tCisco Systems
192.168.1.5\taa:bb:cc:dd:ee:05\t(Unknown)

2 packets received by filter, 0 dropped
```
Only lines where parts[0] matches r'^\d{1,3}(?:\.\d{1,3}){3}$' are data lines.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for baseline.py (RED)</name>
  <files>tests/test_baseline.py</files>
  <read_first>
    - arp_detector/arp_table.py — ARPTable.update() signature and return contract
    - tests/test_detector.py — existing test style and helper patterns to follow
  </read_first>
  <behavior>
    - test_loads_valid_hosts: mock subprocess.run returns MOCK_ARP_SCAN_OUTPUT (2 IP/MAC lines),
      call load_baseline(table), assert table.get_all() has 2 rows with correct IPs and MACs.
    - test_skips_header_footer: same mock output — assert table does NOT contain entries with
      key "Interface:", "Starting", or "packets" (proves header/footer filtering works).
    - test_returns_count: assert load_baseline() return value == 2 (matches the 2 data lines).
    - test_handles_missing_binary: mock subprocess.run raises FileNotFoundError,
      assert load_baseline() returns 0 and does not raise.
    - test_handles_timeout: mock subprocess.run raises subprocess.TimeoutExpired,
      assert load_baseline() returns 0 and does not raise.
    - test_empty_table_on_header_only: mock output contains only header/footer lines (no data),
      assert return value == 0 and table is empty.
    - MOCK_ARP_SCAN_OUTPUT fixture string to use in tests:
      ```python
      MOCK_ARP_SCAN_OUTPUT = (
          "Interface: eth0, type: EN10MB, MAC: aa:bb:cc:00:00:01, IPv4: 192.168.1.1\n"
          "Starting arp-scan 1.9.7 with 256 hosts\n"
          "192.168.1.1\taa:bb:cc:dd:ee:01\tCisco Systems\n"
          "192.168.1.5\taa:bb:cc:dd:ee:05\t(Unknown)\n"
          "\n"
          "2 packets received by filter, 0 dropped\n"
      )
      ```
    - All tests must run without root, without arp-scan installed, and without network.
      Use unittest.mock.patch("subprocess.run") for all subprocess tests.
  </behavior>
  <action>
    Create tests/test_baseline.py with the above test cases. Tests import
    `from arp_detector.baseline import load_baseline` — this module does not exist yet,
    so ALL tests will fail (ImportError counts as RED). Commit with message:
    `test(02-01): add failing tests for baseline.py (RED)`
  </action>
  <verify>
    <automated>python -m pytest tests/test_baseline.py -v 2>&1 | head -30</automated>
  </verify>
  <done>All tests in test_baseline.py fail with ImportError or similar — RED phase confirmed.</done>
  <acceptance_criteria>
    - grep -n "from arp_detector.baseline import load_baseline" tests/test_baseline.py
    - grep -n "MOCK_ARP_SCAN_OUTPUT" tests/test_baseline.py
    - grep -n "FileNotFoundError" tests/test_baseline.py
    - grep -n "TimeoutExpired" tests/test_baseline.py
    - grep -n "patch" tests/test_baseline.py
    - python -m pytest tests/test_baseline.py -q 2>&1 | grep -E "ERROR|FAILED|error"
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Implement baseline.py to make tests pass (GREEN)</name>
  <files>arp_detector/baseline.py</files>
  <read_first>
    - tests/test_baseline.py — the exact tests that must pass
    - arp_detector/arp_table.py — ARPTable.update() call pattern
  </read_first>
  <behavior>
    - load_baseline(table: ARPTable, timeout: int = 30) -> int
    - Run ["arp-scan", "--localnet"] via subprocess.run(capture_output=True, text=True, timeout=timeout)
    - NEVER use shell=True
    - Guard IP lines with: _IP_RE = re.compile(r'^\d{1,3}(?:\.\d{1,3}){3}$')
    - For each line: split by '\t', check len(parts) >= 2 and _IP_RE.match(parts[0].strip())
    - Call table.update(parts[0].strip(), parts[1].strip()) for valid lines
    - Count and return number of valid lines processed
    - FileNotFoundError: print warning to sys.stderr, return 0 (do not crash)
    - subprocess.TimeoutExpired: print warning to sys.stderr, return 0 (do not crash)
  </behavior>
  <action>
    Create arp_detector/baseline.py implementing load_baseline() exactly as specified.
    Module-level docstring must reference DET-02 and state the subprocess contract.
    Import order: stdlib first (re, subprocess, sys), then project (arp_detector.arp_table).
    Do NOT export anything other than load_baseline.
    After implementation, run tests and commit only when all tests pass:
    `feat(02-01): implement baseline.py — arp-scan wrapper seeds ARPTable (GREEN)`
  </action>
  <verify>
    <automated>python -m pytest tests/test_baseline.py -v</automated>
  </verify>
  <done>All test_baseline.py tests pass. Full suite still passes (no regressions).</done>
  <acceptance_criteria>
    - grep -n "def load_baseline" arp_detector/baseline.py
    - grep -n "shell=True" arp_detector/baseline.py | wc -l  # must be 0
    - grep -n "_IP_RE" arp_detector/baseline.py
    - grep -n "FileNotFoundError" arp_detector/baseline.py
    - grep -n "TimeoutExpired" arp_detector/baseline.py
    - python -m pytest tests/test_baseline.py -v 2>&1 | grep -E "passed|PASSED"
    - python -m pytest tests/ -q 2>&1 | tail -3
  </acceptance_criteria>
</task>

</tasks>

<verification>
Full suite (existing 28 tests + new test_baseline.py tests) passes:

```
python -m pytest tests/ -v
```

No regressions in test_arp_table.py, test_detector.py, or test_capture.py.
baseline.py contains no shell=True (grep confirms zero occurrences).
</verification>

<success_criteria>
- arp_detector/baseline.py exists and is importable
- load_baseline() parses arp-scan output correctly (header/footer skipped, data lines loaded)
- load_baseline() handles FileNotFoundError and TimeoutExpired without crashing
- All test_baseline.py tests pass
- Full test suite green (no regressions)
- DET-02 requirement addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-detection-pipeline-alerting/02-01-SUMMARY.md`
</output>
